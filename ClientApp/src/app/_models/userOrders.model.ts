import { Car } from './car.model';

export class UserOrders {
    CarId: number[];
    OrderIdCode: string;
    OrderIdList: number [];
    userId: number;
    colorClass: string;
    carsOrdered: Car;
    }